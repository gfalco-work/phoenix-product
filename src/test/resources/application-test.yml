spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ""
          jwk-set-uri: https://idp.example.com/.well-known/jwks.json

  flyway:
    enabled: false
  r2dbc:
    pool:
      enabled: false